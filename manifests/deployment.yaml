apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapplication
  labels:
    app: webapplication
  namespace: namespace-workflow-1687866844057
spec:
  replicas: 1
  selector:
    matchLabels:
      app: webapplication
  template:
    metadata:
      labels:
        app: webapplication
    spec:
      containers:
        - name: webapplication
          image: acrworkflow1687866844057.azurecr.io/image-workflow-1687866844057:latest
          ports:
            - containerPort: 80