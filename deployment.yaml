apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapplication
  labels:
    app: webapplication
  namespace: namespace-workflow-1688103216000
spec:
  replicas: 1
  selector:
    matchLabels:
      app: webapplication
  template:
    metadata:
      labels:
        app: webapplication
    spec:
      containers:
        - name: webapplication
          image: acrworkflow1688103216000.azurecr.io/image-workflow-1688103216000:latest
          ports:
            - containerPort: 80
